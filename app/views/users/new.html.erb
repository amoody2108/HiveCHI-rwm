<style>
  body {
  display: flex;
  min-height: 100vh;
  flex-direction: column;
  }
  main{
    flex: 1 0 auto;
  }
</style>
<header>
  <div class="navbar-fixed">
    <nav class="light-blue">
      <div class="nav-wrapper">
        <h4 class="center">Create an Account</h4>
      </div><!-- /header -->
    </nav>
  </div>
</header>
<main>
  <% if ENV["DISABLE_REGISTRATIONS"] == "TRUE" %>
    We’re sorry, but we have temporarily disabled registration for new users while we investigate an issue with the system. Please try registering again later.
  <% else %>
    <div class="row">
      <div class="col s12">
          <ul class="tabs">
            <li class="tab col s3"><a href="#student" class="active" data-theme="a" data-ajax="false">Student</a></li>
            <li class="tab col s3"><a href="#parent" data-theme="a" data-ajax="false">Parent</a></li>
            <li class="tab col s3"><a href="#service-provider" data-theme="a" data-ajax="false">Service Provider</a></li>
          </ul>
      </div>
      <div id="student" class="col s12">
        <%= render partial: "/students/form" %>
      </div>
      <div id="parent" class="col s12">
        <%= render partial: "/parents/form" %>
      </div>
      <div id="service-provider" class="col s12">
        <%= render partial: "/service_providers/form" %>
      </div>
    </div>
  <% end %>
</main>
<footer class="page-footer grey lighten-1">
  <%= render "shared/navbar" %><!-- /navbar -->
</footer><!-- /footer -->
